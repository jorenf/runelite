plugins {
    id 'java'
    id 'com.gradleup.shadow' version '8.3.6'
}

repositories {
    mavenLocal()
    maven {
        url = 'https://repo.runelite.net'
    }
    mavenCentral()
}

def runeLiteVersion = '1.10.39'

dependencies {
    // RuneLite API — provided at runtime by the launcher
    compileOnly group: 'net.runelite', name: 'client', version: runeLiteVersion

    // Lombok — configured manually so it works with any Gradle version (7, 8, 9+)
    compileOnly      'org.projectlombok:lombok:1.18.36'
    annotationProcessor 'org.projectlombok:lombok:1.18.36'

    // Bundled into the shadow jar
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation 'com.google.code.gson:gson:2.10.1'
}

group   = 'com.farmingtracker'
version = '1.0.0'
description = 'OSRS Farming Tracker — tree/fruit-tree webhook notifier'

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

compileJava.options.encoding = 'UTF-8'

jar {
    manifest {
        attributes('Plugin-Version': version)
    }
}

shadowJar {
    archiveClassifier.set('')
}
